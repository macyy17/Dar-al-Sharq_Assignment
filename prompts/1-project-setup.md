Pre-req:
- we need to work in a Laravel + ReactJS project

Current task:
- start a fresh laravel 12 project in this repo with reactjs. i shall share further details

---

Pre-req:
- I want you to create simple project. so that i can make changes also
- it should be clean and well-formatted
- for reactjs components and directory structure planning, checkout `frontend-dev` and `software-architecture` skills for understanding them better.

keep these in mind. in fact, write these to `_notes/sequential/1-guidelines/` after reading skills from `.agents/skills/`

so that we can move next.

---

Pre-req:
- we have some system requirements also. they are attached below.

Current task:
- i want you to write `_notes/seqeutnail/2-system-details/` with a README.md and a DEPS.md
- so that they can be used to start this project.

let me know wehen sequential #2 is ready.

---

current task:
- now install all required deps according to `_notes/seqeutnail/2-system-details/`
- i do not want you to create a native app at all.
- but keep space for artisan commands.
- so that it is fully ready.
- as well as arabic locales and rtl support

---

Pre-req:
- take a look at `./template/` and `./docs/template/`
- it has a complete set of elements that you can use

Current task:
- create all admin and moderator pages
- where they can create and manage pages
- do not create the editor at the moment
- but create all other pages designs.

---

Pre-req:
- we have added the template.
- and created frontend pages designs for admin and moderator pages except the editor
- i want you to refer back to `_notes/seqeutnail/2-system-details/` and `_notes/sequential/1-guidelines/`

Current task:
- fully implement the authentication and functionality of admin and moderator roles
- except the page editing
- also the pages should be crated at end.
- but rendered empty
- also work on scheduled pages as well
- plus react's dynamic rendering.

for now, create a complete plan at `_notes/plans/1-admin-and-moderator.md`.
this plan should have phases:
- each phase with it's db migrations, models, routes and unit tests 
- so that each phase completes a milestone
- there should be finite and less numer of phases. e.g., 5-7 would do.

start now, do not make any changes in project. just create a complete plan.

---

looks like a nice plan. completely implement it and complete the admin panels for creation and publishing pages.

---
